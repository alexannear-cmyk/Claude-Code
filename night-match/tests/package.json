{
  "name": "night-match-tests",
  "private": true,
  "type": "module",
  "description": "Tests run against the shipped ../index.html so they cannot drift from it.",
  "scripts": {
    "test": "node rules.test.mjs && node e2e.mjs",
    "test:rules": "node rules.test.mjs",
    "test:e2e": "node e2e.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
